--- src/app/(app)/gastos/gemini-actions.ts
+++ src/app/(app)/gastos/gemini-actions.ts
@@ -21,6 +21,3 @@
   
   const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY);
-  const model = genAI.getGenerativeModel({ model: "gemini-3.8-flash" });
 
   const prompt = `
@@ -48,7 +45,15 @@
 
+  // Implementar fallback de modelos si hay alta demanda
+  const modelNames = ["gemini-3.8-flash", "gemini-3.7-flash", "gemini-3.6-flash"];
+  
   try {
-    const result = await model.generateContent([
-      prompt,
-      {
-        inlineData: {
-          data: base64Image,
-          mimeType: mimeType
-        }
-      }
-    ]);
-    const response = await result.response;
-    let text = response.text();
+    let result = null;
+    for (const modelName of modelNames) {
+      try {
+        const model = genAI.getGenerativeModel({ model: modelName });
+        result = await model.generateContent([
+          prompt,
+          {
+            inlineData: {
+              data: base64Image,
+              mimeType: mimeType
+            }
+          }
+        ]);
+        break; // Éxito, salir del loop
+      } catch (e: any) {
+        console.warn(\`El modelo \${modelName} falló: \${e.message}\`);
+        if (modelName === modelNames[modelNames.length - 1]) {
+          throw e; // Si es el último modelo, lanzar el error
+        }
+      }
+    }
+
+    if (!result) throw new Error("Todos los modelos fallaron.");
+
+    const response = await result.response;
+    let text = response.text();
